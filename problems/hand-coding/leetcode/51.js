/**
 * @param {number} n
 * @return {string[][]}
 */
var solveNQueens = function(n) {
    let arr = [];

    for (let i = 0; i < n; i++) {
      arr[i] = [];
    }
};

function test(arr, row, n) {
  for (let i = 0; i < n; i++) {
    // test[]
  }
}